name: 更新宏观数据

on:
  schedule:
    # 每天运行4次：UTC时间 0点, 6点, 12点, 18点（北京时间 8点, 14点, 20点, 次日2点）
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch:  # 允许手动运行
  push:
    paths:
      - 'fetch_macro_data.py'  # 当脚本更新时也运行

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v6
      
      - name: 设置Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.10'
      
      - name: 安装依赖
        run: |
          pip install requests pandas yfinance pyfedwatch
      
      - name: 运行数据采集脚本
        run: python fetch_macro_data.py
      
      - name: 查看生成的文件
        run: |
          echo "当前目录文件列表:"
          ls -la
          echo "macro_score.csv 内容:"
          cat macro_score.csv || echo "文件不存在"
      
      - name: 提交更新
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add macro_score.csv
          git diff --quiet && git diff --staged --quiet || (git commit -m "自动更新宏观数据 [skip ci]" && git push)
